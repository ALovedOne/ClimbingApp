'use strict';

var ClimbingApp = ClimbingApp || {};
ClimbingApp.views = ClimbingApp.views || {};

ClimbingApp.views.BaseView = (function() {
  var baseView = function ClimbingApp$BaseView() {
  };

  baseView.prototype = {
  }

  baseView.$inject = ['$scope'];

  return baseView;
})();
